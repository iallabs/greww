#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>

#ifndef EXPORT_H
#define EXPORT_H

// To make sure you don't declare the function more than once by including the header
// multiple times.

char * GPyWrapper(char * function);


#endif
